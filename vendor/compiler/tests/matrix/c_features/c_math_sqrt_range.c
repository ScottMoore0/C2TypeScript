/* Math sqrt at multiple inputs. */
#include <stdio.h>
#include <math.h>
int main(void) {
  printf("%.2f %.2f %.2f %.2f %.2f\n", sqrt(0.0), sqrt(1.0), sqrt(4.0), sqrt(16.0), sqrt(100.0));
  return 0;
}
